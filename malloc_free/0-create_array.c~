#include "main.h"
#include <stdlib.h>

/**
 */

char *create_array(unsigned int size, char c)
{
	unsigned int a = 0;
	char *array = malloc(size * sizeof(c));

	if (size < 0)
		return(NULL);
	else if
		return(array);
	else
		return(NULL);
}
