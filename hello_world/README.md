0) Escriba un script que ejecute un archivo C a través del preprocesador y guarde el resultado en otro archivo.
R//: gcc -E $CFILE

1) Escribe un script que compile un archivo C pero no lo enlace.
R//: gcc -c $CFILE

2) Escribe un script que genere el código ensamblador de un código C y lo guarde en un fichero de salida.
R//: gcc -S $CFILE

3) Escribe un script que compile un fichero C y cree un ejecutable llamado cisfun.
R//: 