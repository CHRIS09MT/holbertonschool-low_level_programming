#include "main.h"

/**
 * mul - multiply two numbers y return the result
 * @a: first number to multiply
 * @b: second number to multiply
 * Return: return the result the of multiplication of two numbers
 */

int mul(int a, int b)
{
	return (a * b);
}
